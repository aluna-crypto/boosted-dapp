import { propNames } from "@chakra-ui/styled-system";
import { memoize } from "@chakra-ui/utils";
/**
 * List of props for emotion to omit from DOM.
 * It mostly consists of Chakra props
 */

var allPropNames = [...propNames, "htmlWidth", "htmlHeight", "textStyle", "layerStyle", "apply", "isTruncated", "noOfLines", "htmlSize", "focusBorderColor", "errorBorderColor", "as", "__css", "css", "sx"];

function createShouldForwardProp(props) {
  var regex = new RegExp("^(" + props.join("|") + ")$");
  return memoize(prop => !regex.test(prop));
}

export var shouldForwardProp = createShouldForwardProp(allPropNames);
//# sourceMappingURL=should-forward-prop.js.map